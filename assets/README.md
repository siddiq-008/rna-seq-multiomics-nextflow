# Assets directory

Add figures here, such as:

```text
workflow_diagram.png
```

Recommended workflow diagram:

SRA FASTQ download → fastp trimming → HISAT2 alignment → featureCounts → eggNOG annotation → multi-omics integration → final CSV
